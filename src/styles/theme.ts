import { blue, green, red } from '@styles/palette';

export const theme = {
  primary: blue[500],
  primaryVariant: blue[600],
  error: red[500],
  errorVariant: red[600],
  success: green[500],
  successVariant: green[600],
};
